apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceest-app-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aceest-app
      track: green
      strategy: blue-green
  template:
    metadata:
      labels:
        app: aceest-app
        track: green
        strategy: blue-green
    spec:
      containers:
        - name: aceest-app
          image: aceest-app:latest
          ports:
            - containerPort: 5000
          envFrom:
            - configMapRef:
                name: aceest-config
          env:
            - name: APP_VERSION
              value: green-candidate
            - name: IMAGE_TAG
              value: green-candidate
            - name: DEPLOYMENT_STRATEGY
              value: blue-green
            - name: RELEASE_COLOR
              value: green
            - name: TRAFFIC_SEGMENT
              value: preview
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
